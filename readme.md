things to add:

- real strategy
- start, reset, do another game
- win conditions and territorial acquisitions (should be min(max radius by unit size, radius until enemy unit is in circle))
- debug mode checkmark
